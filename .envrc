
# Git configuration
git config --local --type bool push.autoSetupRemote true 
# sets a Git configuration option that automatically creates upstream tracking branches when you push.

# Python virtual environment setup - local .venv folder
export VIRTUAL_ENV="$(pwd)/.venv"
export PATH="$VIRTUAL_ENV/bin:$PATH"
unset PYTHONHOME

echo "✅ Activated local Python virtual environment"
echo "🐍 Python: $($VIRTUAL_ENV/bin/python --version)"
echo "📦 Virtual env: $VIRTUAL_ENV"
echo "Done direnv setup!"

# If you have direnv: error .envrc is blocked. Run `direnv allow` to approve its content  
# direnv allow && direnv reload
# known to be Used from Hyd-london-philly-denver-suwanee
